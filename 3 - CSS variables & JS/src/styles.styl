:root
  --base yellow
  --spacing 20px
  --blur 10px

img
  padding var(--spacing)
  background var(--base)
  filter blur(var(--blur))

body
  text-align center
  background #193549
  color white
  font-family "helvetica neue", sans-serif
  font-weight 100